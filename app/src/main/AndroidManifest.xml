<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zerodev.kasremaja">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.zerodev.kasremaja.root.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.zerodev.kasremaja.ui.notification.NotificationActivity"></activity>
        <activity android:name="com.zerodev.kasremaja.ui.webview.WebviewActivity" />
        <activity android:name="com.zerodev.kasremaja.ui.dashboard.DashboardActivity" />
        <activity android:name="com.zerodev.kasremaja.ui.slide.SlideActivity" />
        <activity android:name="com.zerodev.kasremaja.ui.login.LoginActivity" />
        <activity android:name="com.zerodev.kasremaja.ui.splash.SplaceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>